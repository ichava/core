<?php

declare(strict_types=1);

// Mirrors a real pack: no `name`, no `description` -- config.json owns those in
// English. Only the taxonomy labels, which config.json has no equivalent for.
return [
    'variants' => [
        'solid' => 'Solid',
    ],
];
